# API documentation

The OpenAPI 3 export is **generated automatically** from the Go code in Phase 1+ (via `swaggo/swag` or a similar tool) and committed to `openapi.yaml`.

For now, the authoritative source of truth is the endpoint list in `/.claude/plans/expressive-forging-hare.md` § 8.
